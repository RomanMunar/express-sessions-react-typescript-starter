{
  "name": "aaa",
  "version": "1.0.0",
  "description": "---",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\"",
    "pre-commit": "cd api && yarn pre-commit && cd ../vue-client && yarn pre-commit",
    "prepare": "husky install",
    "build": "cd api && yarn build && cd ../react-client && yarn build",
    "start": "cd api && yarn start",
    "installDeps": "cd api && yarn && cd ../react-client && yarn"
  },
  "keywords": [],
  "author": "Roman Munar",
  "license": "ISC",
  "husky": {
    "hooks": {
      "pre-commit": "yarn pre-commit"
    }
  },
  "devDependencies": {
    "husky": "^6.0.0"
  }
}
