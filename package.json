{
  "name": "aaa",
  "version": "1.0.0",
  "description": "---",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\"",
    "pre-commit": "cd backend && yarn pre-commit && cd ../vue-client && yarn pre-commit",
    "prepare": "husky install",
    "build": "cd backend && yarn build && cd ../react-client && yarn build",
    "start": "cd backend && yarn start",
    "installDeps": "cd backend && yarn && cd ../react-client && yarn"
  },
  "keywords": [],
  "author": "Roman Munar",
  "license": "ISC",
  "husky": {
    "hooks": {
      "pre-commit": "yarn pre-commit"
    }
  },
  "devDependencies": {
    "husky": "^6.0.0"
  }
}
